version: '2'
services:

  server:
    build: .
    ports:
      - "80:9000"
    networks:
      - ethereal-network

  redis:
    image: redis
    ports:
     - "6379"
    networks:
      - ethereal-network

networks:
  ethereal-network:
    driver: bridge